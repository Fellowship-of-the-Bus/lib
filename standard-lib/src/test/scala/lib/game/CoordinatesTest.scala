package com.github.fellowship_of_the_bus.lib
package game

import org.scalatest.junit.JUnitRunner
import org.junit.runner.RunWith


/* Test suite for lib/game/Coordinates.scala */
@RunWith(classOf[JUnitRunner])
class CoordinatesTest extends LibTest {
  // TEST //
  test("TEST: Verify full coordinates") (pending)

  // TEST //
  test("TEST: Verify top-left coordinates") (pending)

  // TEST //
  test("TEST: Verify bottom-right coordinates") (pending)

  // TEST //
  test("TEST: Verify center coordinates") (pending)

  // TEST //
  test("TEST: Verify distance function") (pending)
}
